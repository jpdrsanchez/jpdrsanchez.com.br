import styled from 'styled-components'

export const Button = styled.button`
  background: var(--title);
  color: var(--mainBg);
  display: block;
  font-family: var(--titleFont);
  font-weight: 600;
  font-size: 1.125rem;
  line-height: 1.5rem;
  padding: 0.8125rem 1.875rem;
  box-shadow: 2px 2px 20px rgba(20, 20, 20, 0.25);
  border-radius: 0.625rem;
  min-width: 12.5rem;
  text-align: center;
  width: max-content;
  cursor: pointer;
`
